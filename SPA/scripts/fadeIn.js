// Add the 'fade-in' class when the page is loaded
document.addEventListener('DOMContentLoaded', function() {
    var appElement = document.getElementById('app');
    if (appElement) {
        appElement.classList.add('fade-in');
    }
});
